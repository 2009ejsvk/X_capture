{
  "name": "tweet-recomposer-capture",
  "version": "0.1.0",
  "private": true,
  "description": "Small personal web service to rebuild a tweet card and capture it as PNG.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "install:browsers": "playwright install chromium"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "playwright": "^1.51.1"
  }
}

